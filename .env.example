# Copy to .env and fill values (never commit .env)

# Required for Telegram delivery
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional: prepend an AI "executive summary" grounded on the same JSON facts
# USE_LLM_SUMMARY=1
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Corporate SSL inspection breaks Yahoo TLS verification (curl error 60). Last resort:
# YFINANCE_SSL_VERIFY=0
